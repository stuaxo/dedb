"""Click commands contributed by the archive app: none. archive.org games
are named `archive://<id>` and driven by the generic commands (see
dedb.verbs, dedb.archive.backend). dedb.core.get_apps() reads `commands`."""

commands: list = []
